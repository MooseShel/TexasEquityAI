:{$PORT}

# Serve static frontend files first
handle {
	root * /app/.web/build/client
	@static file
	handle @static {
		file_server
	}

	# If not a static file, proxy to Reflex backend (API, WebSocket, etc.)
	handle {
		reverse_proxy localhost:8001
	}
}
