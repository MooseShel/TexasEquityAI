name: Deploy to Railway

on:
  push:
    branches: [main]

# Railway auto-deploys from GitHub pushes when connected.
# This workflow exists only as a placeholder / for future CI steps.
# Railway handles the Docker build and deployment automatically.

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Railway auto-deploy triggered
        run: echo "Railway will automatically build and deploy from this push."
